[package]
name = "rust-test"
version = "0.1.0"
authors = ["noxim"]
edition = "2018"
resolver = "2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
vg = { path = "../rust" }

[profile.dev.package."*"]
opt-level = 3